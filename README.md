# vue-server-renderer-test

> Vue.js Server Side Rendering

## Build Setup

``` bash
# install dependencies
yarn install

# serve with hot reload at localhost:8080
yarn run dev

# server with SSR at localhost:3000
yarn run serve

# build for production with minification
yarn run build
yarn start
```
